# The time client reads its configuration once, at start, and part of that
# configuration is a symlink into the provisioning generation for this boot. So
# it starts after the generation has been chosen. Both units run before
# sysinit.target, where nothing orders them relative to each other by default.

[Unit]
After=brenn-config-select.service

# The device asks names, it does not answer them. Link-local name resolution
# and multicast DNS both open listeners on every link, and sshd is meant to be
# the only listener this image has.

[Resolve]
LLMNR=no
MulticastDNS=no
